<div class="tab_contents_header" id="tab_contents_${parameters.id}">
    <div class="tab_contents"
         id="tab_contents_update_${parameters.id}"
	 #if ($parameters.remote)
	      dojoType='BindDiv'
	      #if ($parameters.href) href="$!webwork.htmlEncode($parameters.href)" #end
	      #if ($parameters.loadingText) loadingHtml="$!webwork.htmlEncode($parameters.loadingText)" #end
	      #if ($parameters.errorText) errorHtml="$!webwork.htmlEncode($parameters.errorText)" #end
	      #if ($parameters.showErrorTransportText) showTransportError="true" #end
	      #if ($parameters.delay) delay="$!parameters.delay" #end
	      #if ($parameters.updateFreq) refresh="$!parameters.updateFreq" #end
	      #if ($parameters.listenTopics) listenTopics="$!webwork.htmlEncode($parameters.listenTopics)" #end
	      #if ($parameters.afterLoading) onLoad="$!webwork.htmlEncode($parameters.afterLoading)" #end
     #end
 	 #if ($parameters.cssClass) class="$!webwork.htmlEncode($parameters.cssClass)" #end
	 #if ($parameters.cssStyle) style="$!webwork.htmlEncode($parameters.cssStyle)" #end
      >
